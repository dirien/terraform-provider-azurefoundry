---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "azurefoundry_file_v2 Resource - terraform-provider-azurefoundry"
subcategory: ""
description: |-
  Uploads a local file to the Azure AI Foundry project.
  The resulting file ID can be used with azurefoundry_vector_store (for
  file_search) or passed directly in code_interpreter_file_ids on an
  azurefoundry_agent.
  ~> Note Files are immutable in the Foundry API. If you change source,
  Terraform will delete the old file and upload a new one (replace).
---

# azurefoundry_file_v2 (Resource)

Uploads a local file to the Azure AI Foundry project.

The resulting file ID can be used with `azurefoundry_vector_store` (for
`file_search`) or passed directly in `code_interpreter_file_ids` on an
`azurefoundry_agent`.

~> **Note** Files are immutable in the Foundry API. If you change `source`,
Terraform will delete the old file and upload a new one (replace).

## Example Usage

```hcl
resource "azurefoundry_file" "knowledge" {
  source = "./docs/knowledge.pdf"
}

output "file_id" {
  value = azurefoundry_file.knowledge.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `source` (String) Path to the local file to upload. Changing this forces a new file to be uploaded.

### Optional

- `purpose` (String) The intended use of the file. Currently only `assistants` is supported. Defaults to `assistants`.

### Read-Only

- `bytes` (Number) Size of the uploaded file in bytes.
- `created_at` (Number) Unix timestamp when the file was uploaded.
- `filename` (String) The filename as stored in the Foundry service.
- `id` (String) The file ID assigned by the Foundry service.
